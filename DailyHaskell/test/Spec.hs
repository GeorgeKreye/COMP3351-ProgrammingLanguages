module Spec where -- < needs to be removed to be usable for some reason
    {-# OPTIONS_GHC -F -pgmF hspec-discover #-}
    --TODO: find out why this doesn't compile, but the one that has a ioctl error does